'use client'

export default function ThreeBackground({ 
  mousePosition, 
  scrollY 
}: { 
  mousePosition: { x: number, y: number }
  scrollY: number 
}) {
  // All 3D animations removed for cleaner UI
  return null
}
